export const getDomainParts = (email) => email.split('@')[1].split('.');
export const isAdmin = (email) => ['jacopo@ferrioli.eu','postmaster@ferrioli.eu','amministrazione.generale@cas.ferrioli.eu'].includes(email);
